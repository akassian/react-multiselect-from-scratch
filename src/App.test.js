test.todo('TODO: add tests for single, multi, controlled single, controlled multi. \
  Test clicking outside to close, in single select click an option to close \
  Test that placeholder is replaced by selections \
  Test None / Select All / Deselect all works \
  and that it auto-switches if user manualls selects/deselects all \
  Snapshots as well');
